<%- include('partials/head') %>
    <%- include('partials/testing') %>

        <section class="heading-container">
            <h1> Ontdek jouw Digital Hand and Footprint met SDG's </h1>
            <article class="button-container">
                <a href="#sdgs">Calculator</a>
                <a href="/SDGreadmore">Informatie</a>
            </article>
            <p> DSG Statistieken </p>
        </section>

        <h3 id="sdgs">sdgs</h3>

        <span id="counter">0</span>
        <span id="total">van de 10</span>

        <article>
            <% sdgs.forEach(function(sdg) { %>
                <label>
                    <input type="checkbox" class="checkbox">
                    <img src="https://fdnd-agency.directus.app/assets/<%= sdg.icon %>" alt="<%= sdg.title %>"
                        height="175px" width="175px">
                </label>
                <% }) %>
        </article>


        <script src="script.js"></script>

        <!-- <article>
            <% sdgs.forEach(function(sdg) { %>
                <label>
                    <input type="checkbox" class="checkbox">
                    <img src="https://fdnd-agency.directus.app/assets/<%= sdg.icon %>" alt="<%= sdg.title %>"
                        height="175px" width="175px">
                </label>
            <% }) %>
        </article>
        <span id="counter">0</span>
        <span id="total">van de 10</span> -->

        </body>

        </html>